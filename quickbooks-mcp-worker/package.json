{
  "name": "quickbooks-mcp-worker",
  "version": "0.1.0",
  "description": "QuickBooks MCP Worker for Claude integration",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "workers-mcp docgen src/index.ts && wrangler deploy",
    "setup": "npx workers-mcp setup",
    "install:claude": "npx workers-mcp install:claude quickbooks-mcp https://quickbooks-mcp-worker.your-account.workers.dev"
  },
  "dependencies": {
    "workers-mcp": "^0.0.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
